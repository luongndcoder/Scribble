# Phase 1 (REVIEW GATE): Loader tests

**HARD GATE — user review bắt buộc trước impl.**

Trình bày cho user:
- 7 test cases (`test_diarize_loader.py`) + lý do mỗi case.
- Confirm node-name introspection là cách đúng (không hardcode `feats`/`embs`).
- Confirm fallback voxceleb là hành vi mong muốn.
- Xác nhận test data fixture (`sample_16k.wav`) chấp nhận được.

User approve → sang `phase-01-impl-loader.md`.
User reject → sửa test theo feedback, re-review.

> Không viết impl trước khi gate này pass.
