# Phase 14 — TEST REVIEW GATE: Settings

**Slice E · user gate.** blockedBy: 13

## Checklist review
- [ ] `device-info` payload đủ cho UI (tier + lý do + model status)?
- [ ] Keys mới additive, default-on-read, không migration?
- [ ] UI: tab "Local" không có API key field, có progress + retry + trạng thái version?
- [ ] i18n đủ (vi/en) cho label + trạng thái download?
- [ ] Event `local-model-progress` được FE lắng đúng?

## Output
Approve → Phase 15. Reject → sửa Phase 13.
