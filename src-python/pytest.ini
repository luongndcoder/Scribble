[pytest]
# Sidecar source lives at src-python root; add it to sys.path so tests can
# `import local.device_detect`, `import stt`, etc. without an installed package.
pythonpath = .
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q
